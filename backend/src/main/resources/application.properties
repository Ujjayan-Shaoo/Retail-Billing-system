spring.application.name=retail-billing

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/retail_billing?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ujjayan
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server
server.port=8080

# JWT
app.jwt.secret=RetailBillingSecretKey2024SuperSecureKeyForJWTSigning1234567890ABCDEF
app.jwt.expiration=86400000

# App
app.low-stock-threshold=10
